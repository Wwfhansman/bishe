// This is a basic Flutter widget test.
//
// To perform an interaction test, add Dart tests in the test directory.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test', (WidgetTester tester) async {
    // Placeholder test 
    expect(1 + 1, equals(2));
  });
}
